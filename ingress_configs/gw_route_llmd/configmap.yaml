apiVersion: v1
kind: ConfigMap
metadata:
  name: openshift-ai-inference-config
  namespace: openshift-ingress
data:
  service: |
    metadata:
        annotations:
            service.beta.openshift.io/serving-cert-secret-name: "openshift-ai-inference-tls"
    spec:
        type: ClusterIP
