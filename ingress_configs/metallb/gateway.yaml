apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: llmd-gateway
  namespace: llm-demo
spec:
  gatewayClassName: metallb-gatewayclass  # or your class
  listeners:
  - name: http
    protocol: HTTP
    port: 80
    allowedRoutes:
      namespaces:
        from: Same